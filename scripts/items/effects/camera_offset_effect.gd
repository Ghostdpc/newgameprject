## 職責：camera_offset 效果 —— 相機機位偏移（接口預留，待 CameraSystem 實現）

class_name CameraOffsetEffect
extends ItemEffect

func apply(ctx: ItemContext) -> void:
	push_warning("CameraOffsetEffect: not yet implemented")

func revert(ctx: ItemContext) -> void:
	pass
